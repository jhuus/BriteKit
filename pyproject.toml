[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "britekit"
dynamic = ["version"]
description = 'Core functions for bioacoustic recognizers.'
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
keywords = []
authors = [
  { name = "Jan Huus", email = "jhuus1@gmail.com" },
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python :: Implementation :: CPython",
]
dependencies = [
  "click>=8.1,<8.4",

  "numpy>=2.0,<2.5; python_version < '3.14'",
  "numpy>=2.3.3,<2.5; python_version >= '3.14'",

  "onnx>=1.18,<2.0",

  "pandas>=2.0,<2.5; python_version < '3.14'",
  "pandas>=2.3.3,<2.5; python_version >= '3.14'",

  "pyinaturalist>=0.20,<1.0",
  "soundfile>=0.13,<1.0",

  "torch>=2.5,<2.12",
  "torchaudio>=2.5,<2.12",
  "torchvision>=0.20,<0.27",

  "librosa>=0.11,<1.0",
  "lightning>=2.5,<2.7",

  "matplotlib>=3.8,<3.11; python_version < '3.14'",
  "matplotlib>=3.10.5,<3.11; python_version >= '3.14'",

  "omegaconf>=2.3,<3.0",

  "scipy>=1.13,<2.0; python_version < '3.14'",
  "scipy>=1.16.1,<2.0; python_version >= '3.14'",

  "tensorboard>=2.19,<3.0",
  "timm>=1.0.17,<2.0",
  "yt-dlp>=2025.6.25",

  "faiss-cpu>=1.10; python_version < '3.14'",
  "faiss-cpu>=1.12; python_version >= '3.14'",
]

[project.scripts]
britekit = "britekit.cli:cli"

[project.urls]
Documentation = "https://github.com/jhuus/BriteKit#readme"
Issues = "https://github.com/jhuus/BriteKit/issues"
Source = "https://github.com/jhuus/BriteKit"

[tool.hatch.version]
path = "src/britekit/__about__.py"

[tool.hatch.envs.britekit]
dependencies = [
  "pytest>=8.3.5",
  "black>=25.0.0",
  "ruff>=0.11.12",
  "mypy>=1.0.0",
  "twine>=6.1.0",
  "types-PyYAML",
  "types-requests",
]

post-install-commands = [
  "pip install -e .",
]

[tool.hatch.envs.britekit.scripts]
test = "pytest {args:tests}"
format = "black src tests"
lint = "ruff check src tests"
typecheck = "mypy src tests"
check = "black --check src tests && ruff check src tests && mypy src tests"

[tool.hatch.build]
include = ["src"]

[tool.hatch.build.targets.wheel]
packages = ["src/britekit"]

# Ensure non-Python sample assets are included in the wheel so
# importlib.resources.files("britekit.install") can access them.
[tool.hatch.build.targets.wheel.force-include]
"install" = "britekit/install"

[tool.hatch.build.targets.sdist]
only-include = [
  "src/britekit",
  "install",
]

[tool.hatch.build.targets.sdist.force-include]
"install" = "britekit/install"

[tool.coverage.run]
source_pkgs = ["britekit", "tests"]
branch = true
parallel = true
omit = [
  "src/britekit/__about__.py",
]

[tool.coverage.paths]
britekit = ["src/britekit", "*/britekit/src/britekit"]
tests = ["tests", "*/britekit/tests"]

[tool.coverage.report]
exclude_lines = [
  "no cov",
  "if __name__ == .__main__.:",
  "if TYPE_CHECKING:",
]
